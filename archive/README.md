# Archive

This directory contains historical snapshots and experiments that remain in the repository for reference only.

Content under `archive/` is not part of the maintained source surface, is excluded from normal lint/test collection, and should not be treated as active production or demo code.

If archived material needs to become active again, restore it into `src/` and `tests/` through a dedicated change that re-establishes ownership, validation, and documentation.
