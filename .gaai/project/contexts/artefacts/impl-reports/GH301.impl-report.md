---
id: GH301
type: impl-report
story: .gaai/project/contexts/artefacts/stories/GH301.story.md
plan: .gaai/project/contexts/artefacts/plans/GH301.execution-plan.md
created_at: 2026-04-20
commit: 3597313
---

# GH301 Implementation Report

## Summary

Delivered all four UX polish features for the Workout Tracker: muscle-group tags, light/dark theme toggle, undo toasts with soft-delete, and keyboard shortcuts.

## Changes Made

| File | Change | Purpose |
|------|--------|---------|
| `src/workout_tracker/schema.sql` | Added `muscle_tags TEXT`, `deleted_at TEXT` to exercises; `deleted_at TEXT` to workouts and sets | Schema for tags + soft-delete |
| `src/workout_tracker/models.py` | Added `muscle_tags: str | None` field to `Exercise` | Model update for tags |
| `src/workout_tracker/db.py` | `init_db` migration helper; soft-delete on `delete_exercise/workout/set`; `restore_*` methods; `update_exercise_tags`; filter `deleted_at IS NULL` in all list/get queries; `_get_set_raw` for internal use | Full repository layer update |
| `src/workout_tracker/app.py` | New endpoints: `PUT /api/exercises/<id>/tags`, `POST /api/exercises/<id>/restore`, `POST /api/workouts/<id>/restore`, `POST /api/sets/<id>/restore` | API surface for tags and restore |
| `src/workout_tracker/static/style.css` | Light theme CSS variables under `:root[data-theme="light"]`; tag chip styles; theme toggle button style; undo toast button style | Theme + UI styles |
| `src/workout_tracker/static/app.js` | `initTheme`/`toggleTheme` (prefers-color-scheme + localStorage); undo toast with 5s window; `renderTagChips`; keyboard shortcut handler; `switchToTab` extracted function | All frontend features |
| `src/workout_tracker/templates/index.html` | Added `#theme-toggle` button to topbar `.brand` | Theme toggle button |
| `tests/test_workout_tracker_db.py` | Added `TestMuscleTags` (4 tests) + `TestSoftDelete` (7 tests) | New test coverage |
| `tests/test_workout_tracker_app.py` | Added `TestExerciseTagsRoute` (4 tests) + `TestSoftDeleteRestoreRoutes` (3 tests) | New route coverage |

## AC Mapping

| AC | Implementation |
|----|---------------|
| AC1 (issue #301 resolved) | All 4 features delivered: tags, theme, undo toasts, shortcuts |
| AC2 (all new code has tests, TDD) | 18 new tests across db and app test files |
| AC3 (ruff check passes) | Verified — all checks passed |
| AC4 (no new print() in src/) | Verified — grep confirms no print calls |
| AC5 (PR targeting staging) | PR created after QA PASS |

## Test Results

- 46 tests total (28 pre-existing + 18 new)
- 46/46 PASS
- ruff: clean

## Notes

- `utcnow()` deprecation warnings are pre-existing in the codebase (not introduced by this PR)
- Soft-delete for merge_exercise still uses hard DELETE on the source exercise (merge is a consolidation operation, not a destructive user action — consistent with original behavior)
- `get_set` and `get_workout` now filter `deleted_at IS NULL` — consistent with user expectations (deleted items not retrievable by ID)
