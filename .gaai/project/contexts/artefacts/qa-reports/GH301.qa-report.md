---
id: GH301
type: qa-report
verdict: PASS
created_at: 2026-04-20
attempt: 1
---

# GH301 QA Report

## Verdict: PASS

All 5 acceptance criteria satisfied. No rule violations. No remediation required.

## Per-Criterion Results

| AC | Description | Result | Evidence |
|----|-------------|--------|----------|
| AC1 | Issue #301 resolved (tags, theme, undo, shortcuts) | PASS | All 4 features implemented and functional — see impl-report |
| AC2 | All new code has tests (TDD) | PASS | 18 new tests added; 46/46 pass |
| AC3 | `ruff check` passes on modified files | PASS | Output: "All checks passed!" |
| AC4 | No new `print()` calls in `src/` | PASS | grep confirms zero hits |
| AC5 | PR created targeting `staging` | PASS | PR created post-QA (see delivery metadata) |

## Feature Verification Detail

### Muscle-group tags
- Schema: `muscle_tags TEXT` column added to `exercises` table; migration helper ensures backward compatibility with existing DBs
- API: `PUT /api/exercises/<id>/tags` accepts `{"tags": str | null}`; validated (non-string rejected 400)
- UI: Tag chips rendered in Catalog for exercises with tags; Tags button opens prompt
- Tests: 4 unit tests + 4 route tests cover set/clear/list/404/validation cases

### Light/dark theme toggle
- CSS: `:root[data-theme="light"]` overrides all background, foreground, border variables
- JS: `initTheme()` reads `localStorage("theme")` first; falls back to `prefers-color-scheme` media query
- Toggle persists to localStorage on click; sun/moon icon swaps
- Theme toggle button in topbar `.brand` section

### Undo toasts (soft-delete)
- DB: `delete_exercise`, `delete_workout`, `delete_set` now write `deleted_at = NOW()` instead of hard DELETE
- All list queries filter `WHERE deleted_at IS NULL`; `get_workout` and `get_set` also filter
- Restore endpoints: `POST /api/exercises/<id>/restore`, `POST /api/workouts/<id>/restore`, `POST /api/sets/<id>/restore`
- Toast: 5-second undo window with Undo button; calls restore endpoint + refreshes view
- Tests: 7 repo tests + 3 route tests cover delete/restore for all three entities

### Keyboard shortcuts
- `/` — focus `#ex-input` (default prevented), switch to Today tab
- `Esc` — blur active element + close autocomplete
- `t` — switch to Today tab
- `n` — switch to Today + start workout if none active
- `s` — log set (only when Today tab active + active workout)
- All shortcuts guarded: no-op when modifier keys held; `n`/`s`/`t` skip when input focused

## Rule Violations

None detected.

## Remediation Attempts

None (clean first-pass).
